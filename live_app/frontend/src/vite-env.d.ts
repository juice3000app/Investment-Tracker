/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` at build time -- an ISO timestamp
// of when this bundle was built, shown in the Settings dialog so the user
// can confirm which build their browser actually has.
declare const __BUILD_TIME__: string;
